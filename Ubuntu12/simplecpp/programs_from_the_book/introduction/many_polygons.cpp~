#include <simplecpp>
main_program{
  int nsides;

  turtleSim();

  repeat(10){
    cout << "Type in the number of sides: ";
    cin >> nsides;
    repeat(nsides){
        forward(100);
        left(360.0/nsides);
    }
  }
  closeTurtleSim();
}
